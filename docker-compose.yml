# Monitoring Stack - monitoring-pi
# ================================
# Port allocations:
#   3000  - Grafana
#   8428  - VictoriaMetrics
#   3100  - Loki
#   9093  - Alertmanager
#   9115  - Blackbox Exporter
#   8080  - Healthchecks
#   8429  - vmagent

services:

  # victoria-metrics:
  #   image: victoriametrics/victoria-metrics:latest
  #   container_name: victoria-metrics
  #   ports:
  #     - "8428:8428"
  #   volumes:
  #     - vm-data:/storage
  #     - ./victoria-metrics/config:/etc/victoria-metrics
  #   command:
  #     - "--storageDataPath=/storage"
  #     - "--retentionPeriod=${VM_RETENTION_PERIOD:-12}"
  #   restart: unless-stopped

  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER}
  #     - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
  #     - GF_SERVER_ROOT_URL=${GF_SERVER_ROOT_URL}
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #     - ./grafana/provisioning:/etc/grafana/provisioning
  #     - ./grafana/dashboards:/var/lib/grafana/dashboards
  #   restart: unless-stopped

  # loki:
  #   image: grafana/loki:latest
  #   container_name: loki
  #   ports:
  #     - "3100:3100"
  #   volumes:
  #     - loki-data:/loki
  #     - ./loki/config/loki.yml:/etc/loki/local-config.yaml
  #   restart: unless-stopped

  # alertmanager:
  #   image: prom/alertmanager:latest
  #   container_name: alertmanager
  #   ports:
  #     - "9093:9093"
  #   volumes:
  #     - ./alertmanager/config/alertmanager.yml:/etc/alertmanager/alertmanager.yml
  #   restart: unless-stopped

  # blackbox-exporter:
  #   image: prom/blackbox-exporter:latest
  #   container_name: blackbox-exporter
  #   ports:
  #     - "9115:9115"
  #   volumes:
  #     - ./blackbox-exporter/config/blackbox.yml:/config/blackbox.yml
  #   command:
  #     - "--config.file=/config/blackbox.yml"
  #   restart: unless-stopped

  # healthchecks:
  #   image: healthchecks/healthchecks:latest
  #   container_name: healthchecks
  #   ports:
  #     - "8080:8000"
  #   environment:
  #     - SECRET_KEY=${HC_SECRET_KEY}
  #     - SITE_ROOT=${HC_SITE_ROOT}
  #   volumes:
  #     - healthchecks-data:/data
  #   restart: unless-stopped

  # vmagent:
  #   image: victoriametrics/vmagent:latest
  #   container_name: vmagent
  #   ports:
  #     - "8429:8429"
  #   volumes:
  #     - ./vmagent/config/vmagent.yml:/etc/vmagent/vmagent.yml
  #   command:
  #     - "--promscrape.config=/etc/vmagent/vmagent.yml"
  #     - "--remoteWrite.url=http://victoria-metrics:8428/api/v1/write"
  #   restart: unless-stopped

# volumes:
#   vm-data:
#   grafana-data:
#   loki-data:
#   healthchecks-data:
